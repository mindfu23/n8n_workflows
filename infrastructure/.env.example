# n8n Configuration
# Copy this file to .env and fill in your values
# IMPORTANT: Never commit .env to git!

# ===========================================
# REQUIRED SETTINGS
# ===========================================

# Your domain (e.g., n8n.yourdomain.com)
# Use the VM's external IP if you don't have a domain yet
N8N_HOST=n8n.yourdomain.com

# Authentication credentials
# Generate a secure password with: openssl rand -base64 32
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=CHANGE_THIS_TO_A_SECURE_PASSWORD

# ===========================================
# OPTIONAL SETTINGS
# ===========================================

# Timezone (default: America/Los_Angeles)
TIMEZONE=America/Los_Angeles

# CORS settings for frontend integration
# Set to true if your frontend is on a different domain
N8N_CORS_ENABLED=false
N8N_CORS_ORIGIN=https://your-frontend.netlify.app

# ===========================================
# API KEYS (store here for reference, add to n8n UI)
# ===========================================
# These are NOT used by docker-compose, just for your reference
# Add these through the n8n web interface: Settings > Credentials

# ANTHROPIC_API_KEY=sk-ant-...
# OPENAI_API_KEY=sk-...
# STABILITY_API_KEY=sk-...
